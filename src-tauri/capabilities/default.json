{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities Perseus needs: read/write .wiki files and config the user has explicitly chosen via a save/open dialog, clipboard write for 'Copy Persian Wikitext', reading the app's own version, and opening the GitHub Releases page in the default browser.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:app:allow-version",
    "fs:default",
    "fs:allow-read-text-file",
    "fs:allow-write-text-file",
    "fs:allow-exists",
    "fs:allow-mkdir",
    "fs:scope-appconfig",
    "fs:scope-appconfig-recursive",
    "dialog:default",
    "dialog:allow-save",
    "dialog:allow-open",
    "clipboard-manager:allow-write-text",
    "shell:allow-open"
  ]
}
